/*
 * DisplayedBooks Messages
 *
 * This contains all the text for the DisplayedBooks component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DisplayedBooks';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DisplayedBooks component!',
  },
});
