/*
 * RouterButton Messages
 *
 * This contains all the text for the RouterButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RouterButton';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RouterButton component!',
  },
});
